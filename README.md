# dgx-spark-planner
Plan and compare pre-training, fine-tuning, RL, and inference of LLMs on any single accelerator (defaults to the NVIDIA DGX Spark / GB10). Tokens, days, memory, tokens/sec — with the math shown.
